import { UserForm } from '../widgets/user-form';

export const Component = () => {
  return (
    <div className="flex flex-col h-full max-w-2xl mx-auto py-4 items-center justify-center">
      <UserForm />
    </div>
  );
};
